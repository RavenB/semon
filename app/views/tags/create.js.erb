<% if @tag.errors.present? %>
  alert('ERROR');
<% else %>
  $('#tag_t_name').val('');
  $('#tree').data('treeview').remove();

  var treeData = JSON.parse('<%=j @tags_json %>');
  createTreeview('#tree', treeData);
<% end %>
